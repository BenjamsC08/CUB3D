
minimap
	draw the minimap in the corner top left
	minimap center is always the player and 7-8 block around him so the map rotate not the player
	give a color to 0 in the minimap to make the diff between 0 and ' '
